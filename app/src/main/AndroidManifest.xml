<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.vinillos_app_misw">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VinillosappMISW"
        android:usesCleartextTraffic="true"
        tools:targetApi="34">
        <activity
            android:name=".presentation.ui.views.collector.collectorDetail.CollectorDetailActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.views.album.addNewTrack.AddNewTrackActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.views.album.addNewAlbum.AddNewAlbumActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.views.artist.artistDetail.ArtistDetailActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.views.album.albumDetail.AlbumDetailActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.views.home.HomeActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.views.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>